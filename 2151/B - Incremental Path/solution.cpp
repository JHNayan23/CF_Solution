#include <bits/stdc++.h>using namespace std; #define int long long int#define endl '\n'#define all(x) x.begin(), x.end()const int inf = 1e12; void solve(){    int n, m;    cin >> n >> m;    string s;    cin >> s;    vector<int> a(m);    set<int> st;    for (int i = 0; i < m; i++)    {        int x;        cin >> x;        st.insert(x);    }    int cnt = 1;    for (char x : s)    {        cnt++;        if (x == 'B')        {            while (st.count(cnt))                cnt++;        }        st.insert(cnt);        if (x == 'B')        {            while (st.count(cnt))                cnt++;        }    }    cout << st.size() << endl;    for (auto x : st)        cout << x << " ";    cout << endl;}int32_t main(){    ios_base::sync_with_stdio(0);    cin.tie(0);    int t = 1;    cin >> t;    while (t--)        solve();    return 0;}