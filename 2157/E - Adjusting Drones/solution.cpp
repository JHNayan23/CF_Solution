// #include <bits/stdc++.h>// using namespace std; // #define int long long int// #define endl '\n'// #define all(x) x.begin(), x.end() // void solve()// {//     int n, k; cin >> n >> k;//     int a[n];//     map<int,int> mp; //     for(int i = 0; i < n; i++){//         cin >> a[i];//         mp[a[i]]++;//     } //     vector<int> boro, choto;//     for(auto & [f, s] : mp){//         if(s > k) boro.push_back(s);//         else choto.push_back(s);//     } //     int ans = 0;//     if(choto.empty()){//         for(int i = 0; i < boro.size(); i++) ans += (boro[i] - k);//         cout << ans << '\n';//         return;//     } //     for(int i = 0; i < boro.size(); i++){//         ans += (boro[i] + 1) / 2;//         for(int j = 0; j < choto.size(); j++){//             if(boro[i] + choto[j] > k) ans++;//         }//     } //     cout << ans << '\n';// } // int32_t main()// {//     ios_base::sync_with_stdio(0);//     cin.tie(0);//     int t = 1;//     cin >> t;//     while (t--)//         solve();//     return 0;// } #include <bits/stdc++.h>using namespace std; #define int long long int#define endl '\n'#define all(x) x.begin(), x.end() void solve(){    int n, k; cin >> n >> k;    vector<int> fe(5 * n, 0);    for(int i = 0; i < n; i++){        int x; cin >> x;        fe[x]++;    }        int cnt = 0, ans = 0;    for(int i = 1; i <= 3 * n; i++){        if(fe[i] > k){            cnt++;            fe[i + 1] += fe[i] - 1;        }        else{            ans = max(ans, cnt);            cnt = 0;        }    }     cout << ans << '\n';} int32_t main(){    ios_base::sync_with_stdio(0);    cin.tie(0);    int t = 1;    cin >> t;    while (t--)        solve();    return 0;}