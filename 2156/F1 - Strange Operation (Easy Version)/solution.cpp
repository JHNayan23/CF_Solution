#include <bits/stdc++.h>using namespace std; #define int long long int#define endl '\n'#define all(x) x.begin(), x.end() int a[2005], n; void solve(){    cin >> n;    for (int i = 1; i <= n; i++)    {        int x;        cin >> x;        a[x] = i;    }    for (int i = 1; i <= n; i++)        for (int j = 3; j <= n; j++)        {            int x = a[j], y = a[j - 1], z = a[j - 2];            if (x < y and x < z)                a[j - 2] = x, a[j] = y, a[j - 1] = z;        }    for (int i = 1; i <= n; i++)        for (int j = 1; j <= n; j++)            if (a[j] == i)                cout << j << " ";    cout << endl;}int32_t main(){    ios_base::sync_with_stdio(0);    cin.tie(0);    int t = 1;    cin >> t;    while (t--)        solve();    return 0;}