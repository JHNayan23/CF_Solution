#include <bits/stdc++.h>using namespace std; #define int long long#define endl '\n' void solve(){    string s;    cin >> s;     if (s == "first")    {        int n;        cin >> n;        int a[10005];        for (int i = 0; i < n; i++)            cin >> a[i];         string res;        for (int i = 0; i < n; i++)            res.push_back('a' + a[i] - 1);        cout << res << endl;    }    else if (s == "second")    {        string str;        cin >> str;        int n = str.size();        cout << n << endl;        int a[10005];        for (int i = 0; i < n; i++)        {            a[i] = str[i] - 'a' + 1;            cout << a[i] << " ";        }        cout << endl;    }} int32_t main(){    ios::sync_with_stdio(false);    cin.tie(nullptr);    solve();    return 0;}