#include <bits/stdc++.h>using namespace std; #define int long long int#define endl '\n'#define all(x) x.begin(), x.end()#define debug(x) cout << #x << " " << x << '\n' void solve(int test){    int n;    cin >> n;    int k;    cin >> k;    string s;    cin >> s;    int one = count(all(s), '1');    int consone = 0;    for (int i = 0; i < n; i++)    {        if (s[i] == '1')        {            consone++;            if (consone >= k)            {                cout << "NO" << endl;                return;            }        }        else        {            consone = 0;        }    }    if (one == 0)    {        cout << "YES" << endl;        for (int i = 1; i <= n; i++)            cout << i << " ";        cout << endl;        return;    }    cout << "YES" << endl;    int x = n;    vector<int> p(n);    for (int i = 0; i < n; i++)    {        if (s[i] == '0')            p[i] = x, x--;    }    for (int i = 0; i < n; i++)    {        if (s[i] == '1')            p[i] = x, x--;    }    for (int i = 0; i < n; i++)        cout << p[i] << " ";    cout << endl;} int32_t main(){    ios_base::sync_with_stdio(false);    cin.tie(NULL);    cout.tie(NULL);     int tt = 1;    cin >> tt;    for (int t = 1; t <= tt; t++)        solve(t);    return 0;}