#include <bits/stdc++.h>using namespace std; #define int long long int#define endl '\n'#define all(x) x.begin(), x.end() void solve(){    int n;    cin >> n;    vector<int> a(n);    for (int i = 0; i < n; i++)        cin >> a[i];    int ans = 0;    vector<int> dp(n, 0);    dp[0] = 1;    // for(int i=0;i<n;i++){    // int lds=1,x=a[n-1];    // for(int i=n-2;i>=0;i--){    for (int i = 1; i < n; i++)    {        if (a[i] > a[i - 1])            dp[i] = dp[i - 1] + 1;        else            dp[i] = dp[i - 1] + (i + 1);        ans += dp[i];    }    cout << ans + 1 << endl;} int32_t main(){    ios_base::sync_with_stdio(0);    cin.tie(0);    int t = 1;    cin >> t;    while (t--)        solve();    return 0;}