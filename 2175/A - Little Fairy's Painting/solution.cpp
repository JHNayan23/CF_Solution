#include <bits/stdc++.h>using namespace std; #define int long long int#define endl '\n'#define all(x) x.begin(), x.end() void solve(){    int n; cin >> n;    vector<bool> vis(2005, false);    int ans = 0;    for (int i = 0; i < n; ++i)    {        int x;        cin >> x;        if (!vis[x])        {            vis[x] = true;            ans++;        }    }    while (!vis[ans])    {        vis[ans] = true;         ans++;               }     cout << ans << "\n";} int32_t main(){    ios_base::sync_with_stdio(0);    cin.tie(0);    int t = 1;    cin >> t;    while (t--)        solve();    return 0;}