#include <bits/stdc++.h>using namespace std; #define int long long int#define endl '\n'#define all(x) x.begin(), x.end() int make(int n){    int c = 0;    while (n)        n /= 10, c++;    return c;}void solve(){    int x;    cin >> x;    // int xy;    // for (int i = 1; i <= 1e6; i++)    // {    //     int xy = make(i);    //     int p = x * pow(10, xy) + i;    //     if (p % (x + i) == 0)    //     {    //         cout << i << endl;    //         return;    //     }    // }    cout<<2*x<<endl;}int32_t main(){    ios_base::sync_with_stdio(0);    cin.tie(0);    int t = 1;    cin >> t;    while (t--)        solve();    return 0;}