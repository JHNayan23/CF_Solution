#include <bits/stdc++.h>using namespace std; #define int long long#define endl '\n'#define all(x) x.begin(), x.end() void solve(){    int n;    cin >> n;    int a[n];    for (int i = 0; i < n; i++)        cin >> a[i];    for (int i = 2; i <= 200; i++)    {        for (int j = 0; j < n; j++)        {            if (gcd(a[j], i) == 1)            {                cout << i << '\n';                return;            }        }    }    cout << -1 << '\n';} int32_t main(){    ios::sync_with_stdio(false);    cin.tie(nullptr);    int t = 1;    cin >> t;    while (t--)        solve();    return 0;}