#include <bits/stdc++.h>using namespace std; #define int long long int#define endl '\n'#define all(x) x.begin(), x.end() void solve(){    int n, k;    cin >> n >> k;    vector<int> a(n + 1), vis(n + 1);    vector<pair<int, int>> vp;    for (int i = 1; i <= n; i++)    {        cin >> a[i];        vis[a[i]] = i;    }    for (int i = 1; i <= n; i++)        vp.push_back({vis[i], i});    sort(all(vp));    for (int i = 0; i < k; i++)        cout << vp[i].second << " ";    cout << endl;}int32_t main(){    ios_base::sync_with_stdio(0);    cin.tie(0);    int t = 1;    cin >> t;    while (t--)        solve();    return 0;}