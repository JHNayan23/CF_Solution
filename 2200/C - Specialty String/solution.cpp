#include <bits/stdc++.h>using namespace std;#define int long long int#define all(x) x.begin(), x.end() void solve() {    int n; cin >> n;    string s; cin >> s;    vector<char> st;    for (char x : s) {        if (!st.empty() and st.back() == x) st.pop_back();        else st.push_back(x);    }    cout << (st.empty() ? "YES\n" : "NO\n");}int32_t main() {    ios_base::sync_with_stdio(0); cin.tie(0);    int t = 1; cin >> t;    while (t--) solve();    return 0;}