#include <bits/stdc++.h>using namespace std; #define int long long int// #define endl '\n'#define all(x) x.begin(), x.end() void solve(){    int n, x;    cin >> n;    for (int i = 1; i < n; i++)    {        for (int j = 1; j <= n; j++)        {            x = (i + j) % n;             if (x == 0)                x = n;             cout << j << " " << x << endl;             int ret;            cin >> ret;             if (ret)            {                return;            }        }    }}int32_t main(){    ios_base::sync_with_stdio(0);    cin.tie(0);    int t = 1;    cin >> t;    while (t--)        solve();    return 0;}